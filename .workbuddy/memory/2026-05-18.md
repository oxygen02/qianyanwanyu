# 2026-05-18 工作记录

## 落墨页设置面板重构 + 墨迹渲染增强

### 墨迹渲染引擎（ink-effect.js）
- 重写 drawInkBlock，从5层扩展到7层：铅字压痕、纤维渗透、表层晕染、主体实墨、错位重影、飞白破损、纸张高光
- 全部使用 source-over（multiply 在浅色纸张上会把深色墨迹压成灰色，26*232/255=23，几乎不可见）
- 纤维渗透层 shadowBlur = blurRadius * fontSize * 1.6，确保羽化边缘肉眼可见
- 逐字墨色随机变化：variation 控制 RGB 三通道独立偏移，每字不透明度 0.85~0.99 差异
- 套印偏差偏移加大到 0.08*fontSize，2个方向随机
- 飞白破损：白色细线叠加，damage 控制破损字比例

### 设置面板（luomo.wxml / luomo.wxss）
- 从单一纵向列表重构为左右双栏弹窗
- 左栏（字体）：基础排版（字号/行距/字距/方向/字重）、墨色质感（颜色/浓度/变化/浸染）、印刷工艺（套印/破损）
- 右栏（纸张）：基础纸张（底色/边距）、质感效果（老旧/纹理/阴影/光照）、装饰元素（印章/水印位置）
- 紧凑一行布局：label + slider + value，segment 选择器
- 颜色选择器用色块网格（4色墨色 + 6色纸色）

### 逻辑层（luomo.js）
- 扩展 settings 数据结构到 18+ 参数
- _buildSettingsFromTemplate 统一初始化，_getEffectiveTemplate 全面合并
- 新增 14 个 onChange 事件处理函数
- 新增 _resetToolbarTimer 辅助方法简化重复代码

### 其他
- constants.js：10套模板全部添加 misRegistration 和 damage
- renderer.js：_drawPageShadow 支持 shadowIntensity 动态调节，新增左上角微高光
- 所有 JS 文件通过 node --check 语法检查

## 晚班迭代（21:30 起）- 基于用户截图反馈

### UI 命名与唤醒机制优化
- 边缘热区提示词：左"字体设置"、右"模板设置"（原"设置"/"模板"含义模糊）
- 悬浮工具栏标签同步改为"字体设置"/"模板设置"
- 空状态提示改为"点按左边缘唤出字体设置，右边缘唤出模板设置"
- 设置面板标题改为"排版设置"，增加副标题"左栏调字迹，右栏调纸张"
- 边缘热区渐变增强（0.10->0.14）、指示条加长（60->72rpx），提高可发现性

### 版心位置控制（用户核心诉求：第一个字在哪）
- 将单一"边距"slider 拆分为四向独立控制：留白上/下/左/右，各 50~150%
- 替换 settings 中的 marginScale 为 marginTopVal/marginBottomVal/marginLeftVal/marginRightVal
- _getEffectiveTemplate 中独立计算各向 margin，用户可精确控制版心位置

### 墨迹真实感强化
- drawInkBlock 各层 opacity 全面提升：压痕0.50、渗透0.40、晕染0.55
- 主体实墨层 minOp 从 opacity*0.70 提升至 opacity*0.88，避免字变淡发灰
- 新增第3.5层"边缘浸润"：极微 shadowBlur 让字迹边缘不锐利，模拟纤维吸墨
- 错位重影偏移加大（0.10*fontSize）、opacity 提升至0.35
- 飞白破损白线加宽（0.015->0.04）、数量增加（最多4条）
- 高光层 opacity 0.10->0.14
- drawInkChar 同步提升参数

### 水印位置
- constants.js 中所有模板 watermark.position 统一为 bottomRight（原 republican-literati/old-newspaper/bailu-poetry 为 bottomCenter）

## 字体库扩展 + 质感默认项保障

### 字体库（72个字体全部导入）
- constants.js：BUILT_IN_FONTS 从2个扩展到72个（19个家族）
- 命名规范：fontId = `{家族}-{字重}`，如 `上古宋体-Regular`、`思源宋体-Bold`
- 新增 fileSize 字段，用于展示"下载此字体可能产生流量"警告
- 新增 `CLOUD_ENV_ID` 占位符（需用户确认云环境ID）
- 旧字体名 `ShangGuSongTi-*` 全部替换为 `上古宋体-*`（影响 constants.js 模板 + moban-templates.js）

### 字体选择 UI（新增）
- luomo.wxml：左栏顶部新增"选择字体"入口行（显示当前字体名 + 右箭头）
- 新增字体选择弹窗（font-panel）：标题栏 + 流量警告 + 72字体滚动列表
- 每行显示：字体家族名 / 字重（如"Regular"）/ 文件大小 + 预览文字"让文字回到纸上"
- 预览使用动态 font-family：已加载字体用实际 family，未加载用系统 serif 回退
- 选中项显示 * 标记

### 字体选择逻辑（luomo.js）
- 新增 fontList 数据（72条）、fontPanelVisible 状态
- 新增 onOpenFontPanel / onCloseFontPanel / onSelectFont 事件处理
- _preloadFontsInBackground：打开面板时后台加载前20个字体
- _updateFontPreviewStatus：实时更新列表中各字体加载状态
- settings.fontId 替代旧的 weight→family 映射逻辑

### 质感默认项保障（防止"原生字体效果"）
- wxml 质感 slider 下限提升：aging 最低0改为最低10、fiberOpacity 最低0改为最低20
- modern-prose 模板 ageOpacity 从 0.10 提高到 0.20（确保默认有岁月质感）
- 注：slider 从低值向下滑动时实际值会变大（slider值*0.01），下限保障防止用户无意中加强质感而非减弱

### 吾卷 slogan 更新
- wujuan.wxml：新增第二行 slogan"让阅读回到阅读"
